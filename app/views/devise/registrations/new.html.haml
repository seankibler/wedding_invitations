.jumbotron

  %h2 Sign up
  %p 
    You will start with a trial account, no credit card needed. Trial accounts can create up to #{MAX_TRIAL_INVITATIONS} invitations. 
  = form_for(resource, :as => resource_name, :url => registration_path(resource_name), html: {class: 'form-horizontal'}) do |f|
    = devise_error_messages!
    %p
      = f.text_field :first_name, placeholder: 'first name'
    %p
      = f.text_field :last_name, placeholder: 'last name'
    %p
      = f.label :type_bride, class: 'radio inline' do
        = f.radio_button :type, 'Bride'
        Bride
      = f.label :type_groom, class: 'radio inline' do
        = f.radio_button :type, 'Groom'
        Groom
    %p
      = f.email_field :email, autofocus: true, placeholder: 'email'
    %p
      = f.password_field :password, placeholder: 'password'
    %p
      = f.password_field :password_confirmation, placeholder: 'confirm password'
    %p= f.submit "Sign up", class: 'btn btn-large btn-success'
  = render "devise/shared/links"
