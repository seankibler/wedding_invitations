.jumbotron
  %h1 The bread is still in the oven!
  %p Sign up to our newsletter to get notified when we are accepting new accounts.

%h2 Sign up
= form_for(resource, :as => resource_name, :url => registration_path(resource_name), html: {class: 'form-horizontal'}) do |f|
  = devise_error_messages!
  .control-group
    .controls
      = f.text_field :first_name, placeholder: 'first name'
      = f.text_field :last_name, placeholder: 'last name'
  .control-group
    .controls
      = f.select :type, [['Bride', 'Bride'], ['Groom', 'Groom']]
  .control-group
    .controls
      = f.email_field :email, autofocus: true, placeholder: 'email'
  .control-group
    .controls
      = f.password_field :password, placeholder: 'password'
      = f.password_field :password_confirmation, placeholder: 'confirm password'
  %div= f.submit "Sign up", class: 'btn btn-large btn-success'
= render "devise/shared/links"
