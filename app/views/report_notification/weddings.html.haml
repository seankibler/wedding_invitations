%h1 There are #{@weddings.count} weddings as of #{Date.today.to_s(:long)}

%table
  %tbody
    - @weddings.each do |wedding|
      %tr
        %td= wedding.name
        %td= wedding.created_at.to_s(:long)
