%ul.list-inline
  - Filter.all.each do |filter|
    - if @filter.id == filter.id
      = link_to invitations_path, class: 'remove-filter btn btn-primary' do
        \&times;
        \#{filter.name}
    - else
      %button.btn.btn-primary{type: 'button', href: invitations_path(filter: filter.id)}
        = filter.name
