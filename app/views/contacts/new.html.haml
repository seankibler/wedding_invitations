%h1 Have a question or comment?

= form_for @contact do |f|
  .control-group
    .controls
      = f.text_field :name, placeholder: 'name'
  .control-group
    .controls
      = f.email_field :email, placeholder: 'email'
  .control-group
    .controls
      = f.text_area :message, class: 'input-xlarge', rows: 8, placeholder: 'type your message'

  = f.submit 'Send', class: 'btn'
