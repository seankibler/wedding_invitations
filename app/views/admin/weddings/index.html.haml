%h1 Weddings

%table.table.table-striped.table-bordered.table-condensed
  %thead
    %th Title
    %th # Invitations
    %th Wedding Date
    %th Created
  %tbody
    - @weddings.each do |wedding|
      %tr
        %td= wedding.name
        %td= wedding.invitations.count
        %td= wedding.wedding_date.try(:to_s, :long)
        %td= wedding.created_at.try(:to_s, :long)
