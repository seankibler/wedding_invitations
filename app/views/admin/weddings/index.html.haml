%h1 Weddings

%table.table.table-striped.table-bordered.table-condensed
  %tbody
    - @weddings.each do |wedding|
      %tr
        %td= wedding.name
        %td #{wedding.invitations.count} invitations
        %td= wedding.created_at.to_s(:long)
